{
  "name": "pangrama",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "elm": "^0.19.1-3",
    "normalize.css": "^8.0.1",
    "typeface-fira-sans": "0.0.75"
  },
  "scripts": {
    "start": "elm reactor",
    "build": "elm make --optimize --output=build/main.js src/elm/Main.elm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
