{
  "name": "pangrama",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "elm": "^0.19.1-3",
    "postcss-cli": "^7.1.1",
    "postcss-flexbugs-fixes": "^4.2.1",
    "postcss-import": "^12.0.1",
    "postcss-normalize": "^9.0.0",
    "postcss-preset-env": "^6.7.0",
    "postcss-url": "^8.0.0",
    "typeface-fira-sans": "0.0.75"
  },
  "scripts": {
    "start": "elm reactor",
    "build": "elm make --optimize --output=build/main.js src/elm/Main.elm",
    "build-css": "postcss --config postcss.config.js --output=build/main.css src/styles/main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
