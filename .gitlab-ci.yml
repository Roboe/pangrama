image: node:latest

cache:
  paths:
  - node_modules/

before_script:
- npm install

pages:
  stage: deploy
  script:
  - npm run build
  - mkdir artifacts && mv build artifacts/public # See https://gitlab.com/gitlab-org/gitlab-ee/issues/401
  artifacts:
    paths:
    - artifacts
  only:
  - master
